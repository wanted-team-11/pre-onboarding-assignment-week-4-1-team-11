export type LoginProps = {
  email: string;
  password: string;
};

export type SignupProps = {
  email: string;
  password: string;
};

export type SignupFormProps = {
  email: string;
  password: string;
  confirm: string;
};
